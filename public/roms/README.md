# ROM assets

Place your legally owned ROM files here (`.gb`, `.gbc`, `.gba`). Subfolders are supported.

After adding ROMs, run `npm run predev` or `npm run build` to refresh the game library manifest.

Optional: add a `.png` with the same basename as a ROM for cover art in the library.
